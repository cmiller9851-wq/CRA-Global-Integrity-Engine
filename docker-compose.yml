# ========================================================================
# CRA Global Integrity Engine - Docker Compose
# Author: Cory Miller
# License: Apache 2.0
# Year: 2025
# ========================================================================

version: "3.9"

services:
  cra:
    build: .
    container_name: cra_integrity
    volumes:
      - ./logs:/app/logs
    stdin_open: true
    tty: true
    command: python examples/demo_pipeline.py